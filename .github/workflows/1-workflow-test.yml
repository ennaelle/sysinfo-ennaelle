name: collect system information
on: 
# exécution manuelle
    workflow_dispatch:

jobs:
    network-and-system:
        runs-on: ubuntu-latest
        container:
            image: python:3.10
        steps:
            - name: listing directory
              run: ls -la


